import React from 'react';
import ReactDOM from 'react-dom';
import OrionRender from './OrionRender';
import './index.css';

ReactDOM.render(
  <React.StrictMode>
    <OrionRender />
  </React.StrictMode>,
  document.getElementById('root')
);